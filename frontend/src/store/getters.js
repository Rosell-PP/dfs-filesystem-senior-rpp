export default {
    rail: state => {
        return state.rail;
    },

    order: state => {
        return state.order;
    },

    user: state => {
        return state.user;
    },

    isAuthenticated: state => {
        return state.isAuthenticated;
    },

    registered: state => {
        return state.registered;
    },
}
