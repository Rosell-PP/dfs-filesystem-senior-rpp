export default {
    rail: state => {
        return state.rail;
    },

    order: state => {
        return state.order;
    },

    user: state => {
        return state.user;
    },

    token: state => {
        return state.token;
    },

    isAuthenticated: state => {
        return state.isAuthenticated;
    },
}
